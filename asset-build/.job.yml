---
version: 2.0
targets:
  copy-gpu-operator-artifacts:
    commands: ["sh", "-c", "cd /gpu-operator && /gpuoperator-asset-push.sh"]
    owners: ["email:mharkara@pensando.io"]
    area:
    sub-area:
    feature:
    build-dependencies:
      - build-gpu-operator
      - build-gpu-operator-k8s
      - build-gpu-operator-openshift

image:
  bind_dir: "/go/src/github.com/pensando/gpu-operator"
  work_dir: "/go/src/github.com/pensando/gpu-operator"
